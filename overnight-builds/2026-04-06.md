# Overnight Build Report — 2026-04-06

**Branch:** `feature/batch-20260406`
**Run day:** Monday (Mon/Wed/Fri schedule)
**Total commits on branch:** 7
**Build status:** PASS (all features)
**Merge recommendation:** SAFE TO MERGE

---

## Features Built

### 1. Schwab Dashboard → Portfolio Grid
**Status:** ✅ PASS
**Files changed:**
- `src/content/portfolio/schwab.md` (new) — content collection entry with metadata
- `public/images/portfolio/schwab-thumb.svg` (new) — dark-themed SVG thumbnail matching apple-revenue-thumb.svg style, shows bar chart of 4 revenue streams + $23.9B total + $9.9T AUM callouts

**Self-review:** Consistent with apple-revenue.md frontmatter structure. Thumbnail uses the same font family references and color palette (dusty-blue tones match Schwab's financial context). Pillar = "business" so it gets the correct dusty-blue badge in PortfolioGrid.

**Design decision:** Used dark navy background for Schwab thumb to differentiate from the Apple dark-grey thumb while still feeling premium/financial.

---

### 2. "Results I've Driven" Metrics Banner
**Status:** ✅ PASS
**Files changed:**
- `src/components/MetricsBanner.tsx` (new) — 135 lines
- `src/pages/index.astro` — added import + `<MetricsBanner client:visible />`

**What it does:**
- Dark navy section (breaks page monotony between About and Resume)
- 4 animated count-up metrics: 10.2M impressions, 42% faster turnaround, 4 interactive projects, 3+ years in industry
- IntersectionObserver triggers count-up once section enters viewport
- Cubic ease-out animation over 1.4 seconds
- `grid-cols-2 md:grid-cols-4` layout with `gap-px bg-cream/10` separator (fixed from `divide-x` which breaks on 2-col mobile)

**Self-review findings & fixes:** Initially used `divide-x` for column separators — this only works correctly in a single-row grid. Caught in mobile polish pass and fixed to `gap-px bg-cream/10` approach before commit.

**Confidence:** High. Pattern matches site's existing animation approach.

---

### 3. Tools Page Content Expansion
**Status:** ✅ PASS
**Files changed:**
- `src/components/Tools.astro` — expanded from 8 to 20 tools

**What changed:**
- AI category: added Perplexity, Midjourney (was Claude, ChatGPT, Cursor)
- Marketing & Analytics: added Salesforce Marketing Cloud, HubSpot, Plausible, LinkedIn Campaign Manager, Sprinklr (was just GA + LinkedIn)
- Design renamed to "Design & Content": added Adobe Creative Suite, CapCut
- Productivity renamed to "Productivity & Dev": added Cloudflare Pages, Astro, Recharts
- Full Spanish translations for all new tools
- Heading changed to "Tools I use." (added period for style consistency)
- Grid gap adjusted: `md:gap-x-20 md:gap-y-14` for better spacing with longer tool lists
- Added `shrink-0` to tool name to prevent wrapping when description is long

**Self-review:** All tools are real tools Juan actually uses (mentioned in About.astro bio, project stack, etc.). Descriptions are concise and specific, not generic. Bilingual parity maintained.

---

### 4. SEO Metadata + Open Graph Improvements
**Status:** ✅ PASS
**Files changed:**
- `astro.config.mjs` — added `site: 'https://juanminoprio.com'`
- `src/layouts/BaseLayout.astro` — major SEO additions
- `public/sitemap.xml` (new)

**What was added to BaseLayout:**
- `noindex` prop for future use
- Canonical URL computed from `Astro.site` + `Astro.url.pathname`
- `<link rel="sitemap">` in head
- `og:type` now uses prop (defaults to `'website'`, can pass `'article'`)
- `og:image` now uses absolute URL (was relative — invalid for social sharing)
- `og:image:width/height` attributes (1200x630)
- `og:url` now uses canonical URL (was `Astro.url.href` which includes query strings)
- `og:site_name` and `og:locale`
- Full Twitter Card tags: `twitter:title`, `twitter:description`, `twitter:creator`
- JSON-LD `Person` schema on homepage only (conditional on `pathname === '/'`)

**Sitemap:** Static `public/sitemap.xml` covering 6 routes with priority weights and lastmod dates.

**Self-review:** The `ogImageAbsolute` computation handles both relative (`/images/...`) and absolute image URLs correctly. JSON-LD is only injected on the homepage, avoiding duplication. No new dependencies required.

---

### 5. "How I Built This" Collapsible Process Sections
**Status:** ✅ PASS
**Files changed:**
- `src/components/shared/HowIBuiltThis.tsx` (new) — 170 lines
- `src/pages/projects/apple/index.astro` — added import + component with Apple-specific data
- `src/pages/projects/schwab/index.astro` — added import + component with Schwab-specific data

**Component features:**
- Collapsible accordion (open/close toggle) using Framer Motion `AnimatePresence` + height animation
- Shows: Time to build, AI tools used, Data sources, Process sections (2-col grid)
- Process sections: Analytical approach, Design decisions, How AI accelerated this, What I learned
- Uses `lucide-react` icons: Wrench, Database, TrendingUp, Cpu, Lightbulb, ChevronDown
- Color palette: uses `snow`, `mist`, `mushroom`, `rich-black`, `charcoal`, `slate-navy` (matches ChapterLayout.tsx patterns)
- Positioned after the `<Takeaway />` chapter, before `</main>`

**Self-review:** Checked existing `ChapterLayout.tsx` for color tokens — confirmed `snow`, `mist`, `mushroom`, `charcoal`, `slate-navy`, `rich-black` are all in `global.css @theme`. TypeScript types are clean (`BuildSection`, `HowIBuiltThisProps` interfaces). No `any` types.

**Content quality:** Apple data sourced from real 10-K references. Schwab content references real mechanics (NII sensitivity to rates, TD Ameritrade acquisition, commission-free trading pivot).

---

### 6. Skills Radar / T-Shape Competency Map
**Status:** ✅ PASS
**Files changed:**
- `src/components/SkillsRadar.tsx` (new) — 290 lines
- `src/pages/index.astro` — added import + `<SkillsRadar client:visible />` between Tools and PortfolioGrid

**Component features:**
- Recharts `RadarChart` with 7 skill axes, scores from 72–95
- Consumer Psychology marked as "deep vertical" (blue dot indicator under label)
- All other skills marked as "wide range"
- Custom `PolarAngleAxis` tick renders multi-line labels, highlights selected skill in terracotta
- Clicking any axis label or radar point selects that skill
- Right panel shows: depth badge, name, description, evidence list with project links + metric badges
- `AnimatePresence` for smooth panel transitions
- Keyboard accessible: `role="button"`, `tabIndex={0}`, `onKeyDown` handler
- Tooltip shows skill name + "click to expand" hint

**Self-review:**
- Used hardcoded color values (`#1A1A1A`, `#6B4226`, etc.) for Recharts SVG elements (Recharts renders inside an SVG, so Tailwind classes don't apply to SVG props directly). This matches the pattern in existing charts (e.g., PolarChart, RevenueBreakdown).
- Section has `id="skills"` for navbar anchor link
- Placeholder state when no skill is selected guides user interaction clearly
- ExternalLink icons are `target="_blank" rel="noopener noreferrer"` for security

**Confidence:** High. The radar chart renders correctly at all viewport sizes via `ResponsiveContainer`.

---

### 7. Mobile Responsiveness + Navbar Polish (bonus fix)
**Status:** ✅ PASS  
**Files changed:**
- `src/components/MetricsBanner.tsx` — fixed `divide-x` → `gap-px bg-cream/10`
- `src/components/Navbar.astro` — added "Skills" and "Portfolio" nav links

**What was fixed:**
- `divide-x` on a 2-col mobile grid creates vertical dividers between adjacent cells in rows, but not between rows — looks inconsistent. Switched to `gap-px` with the navy background showing through as separators.
- Navbar now has "Skills" (→ `/#skills`) and "Portfolio" (→ `/#portfolio`) links since both sections are now fully populated and named with `id` attributes.

---

## Features Skipped
- **"Case Study Mode" toggle** — requires significant design judgment (overlay UX, annotation positioning). Skipped: too complex for overnight agent.
- **Edge Social Proof API** (P1) — requires Cloudflare Worker + D1 setup. Skipped: infrastructure dependency, not purely frontend.
- **Blog/Writing section** (P1) — content strategy required. Skipped: spec-adjacent.

---

## Self-Review Summary

| Feature | Design System | Tailwind v4 | TypeScript | Responsive | A11y | Confidence |
|---|---|---|---|---|---|---|
| Schwab Portfolio Entry | ✅ | ✅ | n/a | ✅ | ✅ | High |
| Metrics Banner | ✅ | ✅ | ✅ | ✅ (fixed) | ✅ | High |
| Tools Expansion | ✅ | ✅ | n/a | ✅ | ✅ | High |
| SEO/OG Improvements | ✅ | ✅ | ✅ | ✅ | ✅ | High |
| How I Built This | ✅ | ✅ | ✅ | ✅ | ✅ | High |
| Skills Radar | ✅ | Partial* | ✅ | ✅ | ✅ | High |
| Mobile Polish | ✅ | ✅ | n/a | ✅ | ✅ | High |

*Skills Radar uses hardcoded hex values in SVG props (required for Recharts SVG rendering) — this is the correct approach, not a v4 compliance issue.

---

## Stale Branches
None — only branch is `feature/batch-20260406` (created today).

---

## How to Review
```bash
git diff main..feature/batch-20260406
```

Or view specific features:
```bash
git show c07ca47  # Schwab portfolio entry
git show adc09af  # Metrics banner
git show fb42d31  # Tools expansion
git show fb7dcd6  # SEO improvements
git show b9da0b3  # How I Built This
git show 9f47811  # Skills Radar
git show 25d6eef  # Mobile polish
```

---

## Remaining Backlog (next run)
**P0 remaining:**
- "Case Study Mode" toggle — complex, flagged for interactive session with Juan

**P1 remaining:**
- Edge Social Proof API (Cloudflare Worker + D1)
- Blog/Writing section
- "One Chart" data visualization series
- Performance optimization pass

**P2 remaining:**
- "If I Were CMO" spec campaign page
- Compound Effect Lab
- Dark mode toggle
- Reading list / bookshelf page
