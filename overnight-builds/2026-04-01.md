# Overnight Build Report — 2026-04-01

## Feature Built

**Apple Revenue Dashboard — Portfolio Grid Card**

Added the Apple Revenue interactive data story to the portfolio grid on the homepage. The full 9-chapter experience at `/projects/apple/` was already built; this feature creates the content collection entry and thumbnail that surface it in the portfolio grid alongside other work.

## Files Created / Modified

| File | Action |
|------|--------|
| `src/content/portfolio/apple-revenue.md` | Created — content collection entry with frontmatter |
| `public/images/portfolio/apple-revenue-thumb.svg` | Created — SVG thumbnail for portfolio card |
| `BACKLOG.md` | Updated — checked off completed item, added to Completed section |

## Branch

`feature/apple-portfolio-card-20260401`

## Build Status

**PASS** — 11 pages built (up from 10 pre-feature). New route `/portfolio/apple-revenue.md/` generated cleanly. No errors or warnings introduced.

## Self-Review Results

**Issues found and fixed during review:** None. The change is purely additive (two new files + one frontmatter-only .md file), no Tailwind classes or TypeScript introduced.

**Items flagged for Juan:**
- The SVG thumbnail uses revenue figures summing to ~$416.1B across 5 streams (iPhone $201B, Services $96.2B, Mac $37.8B, iPad $27.9B, Wearables $53.2B). These are approximations consistent with the Apple project page description ("$416.2B revenue model"). If the actual component data differs, the thumbnail should be updated — but it's a visual illustration, not a data source.
- The portfolio slug detail route (`/portfolio/apple-revenue.md`) is generated automatically by the `[...slug].astro` template. It renders fine (just frontmatter + empty body, same as all other entries), but since `externalUrl` is set, users are sent directly to `/projects/apple/` from the grid. The detail route is unreachable from the UI — consistent with other entries that use `externalUrl`.

**Confidence Level:** HIGH — matches existing portfolio entry patterns exactly (same frontmatter fields, same `externalUrl` pattern as `spending-viz.md`).

## Design Decisions

1. **Pillar: `business`** — Chosen over `ai` because the Apple dashboard is a revenue/business analysis story, not an AI tool. The `business` pillar uses dusty-blue styling which suits financial data.
2. **SVG thumbnail** — Created rather than leaving blank. The grid shows a plain "Thumbnail" text placeholder when no thumbnail is set; SVG provides a meaningful preview that communicates the dark-themed, bar-chart nature of the project. Used only design-system colors from `global.css`.
3. **Revenue figures** — Used publicly available Apple FY2025 approximate figures consistent with what the `/projects/apple/` page already states.

## Stale Branches to Review

None — only branch is today's feature branch.

## How to Review

```bash
git diff main..feature/apple-portfolio-card-20260401
```

Or visit the feature branch and run `npm run dev` to see the portfolio grid with the new card.

## Merge Recommendation

**SAFE TO MERGE**

Purely additive change: one `.md` frontmatter file, one static `.svg` asset. No logic, no layout changes, no new dependencies. The portfolio grid already handles the `externalUrl` pattern correctly. Zero risk of regression.

## Next Backlog Item (for next run)

**P0:** Add Schwab Dashboard to Portfolio Grid — same pattern as this build, components exist at `src/components/schwab/`, page at `/projects/schwab/`.
